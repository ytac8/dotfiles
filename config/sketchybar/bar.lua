local colors = require("colors")

-- Equivalent to the --bar domain
sbar.bar({
	-- topmost = "window",
	height = 32,
	margin = 0,
	color = colors.catppuccin_base,
	y_offset = 5,
	padding_right = 8,
	padding_left = 8,
})
